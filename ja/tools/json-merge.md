---
layout: tool
title: JSON結合 | ファイル・JSONL・配列・オブジェクトのマージと重複削除
description: JSONファイル、貼り付けたAPIレスポンス、JSONLログをブラウザ内で結合できます。配列連結、オブジェクトキーと共通配列の統合、重複削除数、キー競合確認、整形/圧縮出力、結果コピー、merged.jsonの保存に対応します。
lang: ja
permalink: /ja/tools/json-merge/
canonical_url: /ja/tools/json-merge/
category: data
category_label: データ・ユーティリティ
thumbnail: /assets/thumbs/json-merge.svg
tool_key: json-merge
keywords: [json 結合, json マージ, 複数 json 1つ, json merge, json 配列 結合, json 貼り付け, jsonl 結合]
related_tools: [link-list-cleaner, text-counter, case-converter]
faq:
  - q: JSON結合前にファイルはサーバーへ送信されますか？
    a: いいえ。ファイル選択、解析、結合、プレビュー、ダウンロード生成はすべて現在のブラウザ内で処理されます。
  - q: 配列JSONを複数アップロードすると、どう結合されますか？
    a: 自動モードでは、すべてのルートが配列ならアップロード順に連結されます。
  - q: オブジェクトJSONはどう結合されますか？
    a: 自動モードではJSONオブジェクト同士のキーを統合し、同名キーは後から読み込んだ値で上書きされます。
  - q: ファイル構造が混在している場合は？
    a: 自動モードでは各ルートを配列で包んで保持するため、元構造を崩さず扱えます。必要な出力形式がある場合は、マージ方式を手動で切り替えてください。
  - q: 大きなJSONファイルも結合できますか？
    a: ブラウザメモリ保護のため、1回につき最大50ファイル、合計20 MBまで処理できます。結果が大きい場合、プレビューは一部のみ表示し、ダウンロードファイルには全体を保存します。
  - q: ファイルを選ばずJSONテキストだけ貼り付けても使えますか？
    a: はい。APIレスポンスや小さなJSONを直接貼り付けられます。複数の場合は --- だけの行で区切り、ファイルと一緒に結合できます。
  - q: 選択したJSONファイルを1つだけ外せますか？
    a: はい。ファイル選択後、画面上のファイル一覧から不要なファイルだけ削除してからマージできます。
  - q: JSONLログのように1行に1つのJSONオブジェクトがある入力も使えますか？
    a: はい。JSONLオプションを有効にすると、空行と//で始まる行を除き、各行を1つのJSON値として解析します。
  - q: 圧縮したJSONとして保存できますか？
    a: はい。出力形式で整形表示または圧縮表示を選ぶと、プレビュー、コピー、ダウンロードに同じ形式が使われます。
alternate_urls:
  ko: /tools/json-merge/
  en: /en/tools/json-merge/
  ja: /ja/tools/json-merge/
---

## JSON結合はどんな時に便利？
同じスキーマのJSONが複数に分かれているとき、素早く1つにまとめたい場合に便利です。

例: 分割ログ、バッチ結果、ページ分割された収集データ。

APIレスポンスをページごとに保存した場合や、複数のバッチ結果を1つの`merged.json`にまとめたい場合、外部サーバーへ再アップロードせずローカルブラウザで確認できます。小さなJSONはファイル化せず、直接貼り付けて配列結合やオブジェクト統合の結果を試せます。JSONLログも1行ずつ解析してローカルで結合できます。

## 今日の改善対象にした理由
最近の品質改善は、`link-list-cleaner`、`average-speed-calculator`、`cafe-work-seat-simulator`、`blog-banned-word-checker`、`image-upscaler`、`case-converter`、`grocery-budget-checker`に集中していたため、同じツールの繰り返しを避けました。

`json-merge`はファイルアップロード、直接貼り付け、JSON解析、大容量制限、オブジェクトキー競合など、入力まわりでつまずきやすい流れが多いツールです。「JSONファイルを結合したい」「配列をマージしたい」「貼り付けたAPIレスポンスをローカルで試したい」という検索意図も明確なため、今回の既存ツール改善対象にしました。

## 主な機能
- JSON複数ファイルアップロード
- APIレスポンスやログ断片の直接貼り付け
- 自動モード（配列/オブジェクトを自動判定）
- 手動モード（配列連結・オブジェクトキー結合・ルート配列化）
- `{ "items": [...] }`のような共通配列を持つオブジェクトの自動連結
- 1行1件のJSONL貼り付け入力
- 整形表示または圧縮表示の出力選択
- 結果プレビュー + 結果コピー + `merged.json` ダウンロード
- 配列項目の重複削除数 + オブジェクトキー競合数の確認
- 不正なJSON、手動モードとの構造不一致、ファイル容量超過のエラー案内
- 選択したファイルの個別削除と貼り付けJSONの容量案内
- サンプルデータで結合方式を先に確認

## 使い方
1. JSONファイルを複数アップロード
2. ファイルがない場合はJSONテキストを貼り付けます。複数のJSON文書は `---` だけの行で区切り、JSONLはオプションを有効にして1行1件で貼り付けます。
3. 対象外にしたい選択ファイルを外し、貼り付けJSONがある場合は容量表示を確認します。
4. 結合モードを選択（初期値: 自動）
5. 出力形式を整形表示または圧縮表示から選びます。
6. **JSON結合** ボタンをクリック
7. 結果を確認し、コピーまたは保存します。

## 注意
- オブジェクト結合では同じキーがある場合、後に読み込んだ値が優先されます。
- 手動のオブジェクト結合では、すべてのファイルのルートがJSONオブジェクトである必要があります。構造が合わない場合は結合せずエラーを表示します。
- 配列の重複削除はオブジェクトのキー順をそろえて比較するため、`{ "a": 1, "b": 2 }`と`{ "b": 2, "a": 1 }`は同じ項目として扱います。
- ブラウザメモリ保護のため、1回につき最大50ファイル、合計20 MBまで処理できます。
- アップロードファイルと貼り付けJSONを合わせて20 MBまで処理します。
- JSONLモードでは空行と`//`で始まる行を無視しますが、行末コメント付きJSONは標準JSONではないため解析エラーとして案内します。
- ファイルも貼り付けJSONもない場合、マージボタンは無効になり、入力が空であることを知らせます。
- アップロードしたファイルと結合結果はサーバーへ送信されず、現在のブラウザ内だけで処理されます。

## 関連ツール
- リンク整理: [リンク抽出クリーナー]({{ '/ja/tools/link-list-cleaner/' | relative_url }})
- 文字数確認: [文字数カウンター]({{ '/ja/tools/text-counter/' | relative_url }})
- キー形式整理: [大文字小文字変換]({{ '/ja/tools/case-converter/' | relative_url }})
